const { expect } = require('chai');
const flightService = require('../../src/services/flightService');

describe('Flight Service', () => {
  // Add test cases for getAllFlights and getFlightById
});