const getAllFlights = () => {
    // Implement logic to fetch all flights from the database
    // Example: return FlightModel.find();
  };
  
  const getFlightById = (flightId) => {
    // Implement logic to fetch a specific flight by ID from the database
    // Example: return FlightModel.findById(flightId);
  };
  
  module.exports = {
    getAllFlights,
    getFlightById,
  };